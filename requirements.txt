# Core detection (jd_injection_scanner.py, detect.py) is stdlib-only — no install needed.
#
# The optional layers below enable additional detection paths. They are
# feature-gated: the tools skip gracefully (printing an install hint) when a
# dependency is absent, so you only need what you intend to use.

# Render-gated detection (render_worker_hidden.py): headless-browser computed-style
# extraction of effectively-hidden text. Required for the "tripwire" pass.
playwright>=1.40        # then run: playwright install chromium

# Multimodal & document detection (multimodal.py) -- all individually gated:
# pytesseract>=0.3.10      # OCR image-only postings (also needs the `tesseract` binary)
# Pillow>=10.0             # image handling for OCR + LSB-stego heuristic
# pdfminer.six>=20221105   # PDF hidden-text (white/tiny/off-page) + metadata
# python-docx>=1.1         # DOCX hidden/vanish/white runs + core properties

# Tests / benchmark:
# pytest>=7.0              # run the suite (python3 -m pytest)
# reportlab>=4.0           # only needed to GENERATE synthetic PDF fixtures in the test suite
